// Root build — plugin declarations only (applied in :bridge subproject)
plugins {
    id 'com.android.library' version '9.4.1' apply false
    id 'com.vanniktech.maven.publish' version '0.37.0' apply false
}
